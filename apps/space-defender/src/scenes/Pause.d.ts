import { Scene } from "phaser";
export declare class Pause extends Scene {
  private modeLetterTexts;
  private currentMode;
  constructor();
  create(): void;
  private toggleVectorMode;
  private createRainbowModeText;
  private updateRainbowModeText;
}
